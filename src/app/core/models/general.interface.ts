export interface States {
    id: number,
    nombre: string,
    codigo: string
}

export interface Conferences {
    id: number,
    nombre: string,
    codigo: string
    estado: string
}

export interface Sizes {
    id: number,
    talla: string,
    descripcion: string
}